<section class="page-hero"><div class="wrap"><span class="eyebrow">{{ article.published_at | date: '%B %d, %Y' }}</span><h1 class="display-md">{{ article.title }}</h1></div></section>
<section class="section"><div class="wrap"><div class="prose">{{ article.content }}</div></div></section>